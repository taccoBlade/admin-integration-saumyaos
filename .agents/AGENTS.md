# Custom Rules for Antigravity

- **Git Operations**: Never run `git push`, stage changes, or make commits automatically without first asking the user for explicit approval and receiving confirmation.
