# Memory Domain: Backend

## Features
- **Serverless API Routes**: Minimal Next.js routes handling metadata sitemaps, sitemap XML, and robots.txt.
- **Static Generation Strategy**: Heavy focus on compilation-time rendering to minimize browser hydration latency.
- **Structured JSON loaders**: Server-side parsing of content catalogs (projects, playlists, and tracks) directly from the filesystem.
