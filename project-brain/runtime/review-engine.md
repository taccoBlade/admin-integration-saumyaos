# Runtime Module: Review Engine

- **Responsibility**: Evaluates the changes against the checklists in `reviews/` and outputs a quantitative confidence score.
- **Constraints**: Gated control mechanism (requires average score >= 90).
