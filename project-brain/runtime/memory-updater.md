# Runtime Module: Memory Updater

- **Responsibility**: Performs incremental updates to corresponding `memory/*.md` files after changes are validated.
- **Constraints**: Replaces only the affected sections.
