# Runtime Module: Graph Updater

- **Responsibility**: Re-generates graph data when new modules are added or existing file import structures are changed.
- **Constraints**: Ran automatically after files are compiled.
