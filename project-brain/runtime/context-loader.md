# Runtime Module: Context Loader

- **Responsibility**: Inspects dependency graph maps and loads only the required memory domain files.
- **Constraints**: Keeps input context window footprint minimal.
