# Engineering Standards: Next.js

- **Asset Optimization**: Use `<Image>` for structural graphics to auto-optimize sizes.
- **Metadata Management**: Define static `Metadata` records in page files to optimize SEO indexability.
- **SSG Parameters**: Implement `generateStaticParams` for dynamic routes to pre-compile all case study paths.
