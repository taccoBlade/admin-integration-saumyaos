# Engineering Standards: Naming Conventions

- **Component Files**: Use kebab-case for directories and PascalCase for React files (`FeaturedProjects.tsx`).
- **Functions & Hooks**: Camel-case for all helpers and custom react hooks (`useOS`, `readJsonFile`).
- **Styles**: Stick to Tailwind utility patterns or semantic prefix tags in CSS.
