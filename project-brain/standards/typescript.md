# Engineering Standards: TypeScript

- **Strict Type Checking**: Explicitly define types/interfaces; avoid using `any`.
- **Interface Definitions**: Maintain structured models in `lib/types.ts`.
- **Props Safety**: Ensure Next.js Page components specify proper Type boundaries for parameters.
